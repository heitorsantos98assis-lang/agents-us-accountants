# 51-individual-tax-return-1040-multistate-multiform — Codex Agent

A specialist subagent from the **57 Agents for US Accountants** bundle (HL).

## What this agent does

See `51-individual-tax-return-1040-multistate-multiform.md` for the full description, frameworks, reference tables, and operating flow.

## Installation

### Prerequisites

- [Codex](https://docs.Codex.com/Codex-code) installed and logged in
- A terminal with `unzip`

### Install in 30 seconds

**1. Unzip this archive:**

```bash
unzip 51-individual-tax-return-1040-multistate-multiform.zip
```

**2. Copy the agent into your Codex project:**

```bash
mkdir -p .codex/agents
cp 51-individual-tax-return-1040-multistate-multiform.md .codex/agents/

# OR install globally for all projects:
mkdir -p .codex/agents/
cp 51-individual-tax-return-1040-multistate-multiform.md .codex/agents/
```

**3. Restart Codex** (or run `/agents` to refresh).

**4. Invoke the agent.** Just describe your task — Codex will pick this subagent automatically when relevant, or call it explicitly:

```
Use the individual-tax-return-1040-multistate-multiform subagent to ...
```

## Verifying installation

```bash
ls .codex/agents/   # should list 51-individual-tax-return-1040-multistate-multiform.md
# OR
ls .codex/agents/ # for global install
```

In Codex, run `/agents` to see the agent listed.

## Updating

When a new version is released, re-run the unzip + copy steps above. The new file overwrites the old one.

## Uninstall

```bash
rm .codex/agents/51-individual-tax-return-1040-multistate-multiform.md
# OR
rm .codex/agents/51-individual-tax-return-1040-multistate-multiform.md
```

## Support

- Documentation: [Codex docs — Subagents](https://docs.Codex.com/Codex-code)
- Issues / questions: open an issue at the repo where you bought this bundle.

---

© HL — 57 Agents for US Accountants
